interface:
  display_name: "Reuse-First Architecture"
  short_description: "Reuse-first SOLID design for code changes"
  default_prompt: "Use $reuse-first-architecture before writing code: inspect existing capabilities, choose reuse/adapt/extend/build, then implement and verify the change."

policy:
  allow_implicit_invocation: true
